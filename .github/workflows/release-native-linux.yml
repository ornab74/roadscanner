name: Build pinned native Linux artifacts (liboqs 0.14 only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: python:3.12-slim

    env:
      # SECURITY PIN: Roadscanner intentionally supports liboqs 0.14.0 only.
      # Do not make this configurable from